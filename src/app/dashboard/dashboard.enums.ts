export enum VentMode {
  none = 'none',
  cmv = 'cmv',
  ac = 'ac',
  simv = 'simv',
  pcv = 'pcv',
  ps = 'ps'
}

export enum VentDataType {
  none = 'none',
  sensor = 'sensor',
  control = 'control'
}
